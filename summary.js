alert( 'SomeText' );
confirm( 'Are you single' );
prompt( 'what is your name' );

/*
loclation.reload();
location.href = '';
location.hash = '';

history.back();
history.forward();
history.go();
history.length;

cookies: short-term and persistent

storage:
1. session storage: removed when the session is closed
2. local storage: remain until manually removed (explicitly removed)

difference between sessionStorage and localStorage
difference between cookies vs sessionStorage vs localStorage

browser API search. a lot of APIs are there
*/